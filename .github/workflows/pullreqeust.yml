name: WF3_Over_Pull_Request 

on: 
  repository_dispatch:
    types: [build]
  schedule:
    - cron: "* * * * *"
  pull_request:
    types : [opened, closed, assigned, reopened]

jobs:
  job1-on-ubuntu:
    # name: Run on ubuntu-latest
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string
        run: echo "Ran GA on Pull Request"
      